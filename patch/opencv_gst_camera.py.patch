--- opencv_gst_camera.py	2021-01-31 11:47:29.257749796 +0000
+++ opencv_gst_camera_new.py	2021-01-31 13:13:19.651002252 +0000
@@ -13,9 +13,12 @@ class OpenCvGstCamera(CameraBase):
     # config
     width = traitlets.Integer(default_value=224).tag(config=True)
     height = traitlets.Integer(default_value=224).tag(config=True)
-    fps = traitlets.Integer(default_value=21).tag(config=True)
-    capture_width = traitlets.Integer(default_value=816).tag(config=True)
-    capture_height = traitlets.Integer(default_value=616).tag(config=True)
+#    fps = traitlets.Integer(default_value=21).tag(config=True)
+    fps = 30
+#    capture_width = traitlets.Integer(default_value=816).tag(config=True)
+#    capture_height = traitlets.Integer(default_value=616).tag(config=True)
+    capture_width = 640
+    capture_height = 480
 
     def __init__(self, *args, **kwargs):
         self.value = np.empty((self.height, self.width, 3), dtype=np.uint8)
@@ -47,7 +50,9 @@ class OpenCvGstCamera(CameraBase):
                 break
                 
     def _gst_str(self):
-        return 'nvarguscamerasrc ! video/x-raw(memory:NVMM), width=%d, height=%d, format=(string)NV12, framerate=(fraction)%d/1 ! nvvidconv ! video/x-raw, width=(int)%d, height=(int)%d, format=(string)BGRx ! videoconvert ! appsink' % (
+#        return 'nvarguscamerasrc ! video/x-raw(memory:NVMM), width=%d, height=%d, format=(string)NV12, framerate=(fraction)%d/1 ! nvvidconv ! video/x-raw, width=(int)%d, height=(int)%d, format=(string)BGRx ! videoconvert ! appsink' % (
+#                self.capture_width, self.capture_height, self.fps, self.width, self.height)
+        return 'v4l2src device=/dev/video0 ! video/x-raw, width=%d, height=%d, framerate=(fraction)%d/1 ! videoscale ! video/x-raw, width=(int)%d, height=(int)%d ! videoconvert ! appsink' % (
                 self.capture_width, self.capture_height, self.fps, self.width, self.height)
     
     def start(self):
@@ -69,4 +74,4 @@ class OpenCvGstCamera(CameraBase):
         
     @staticmethod
     def instance(*args, **kwargs):
-        return OpenCvGstCamera(*args, **kwargs)
\ No newline at end of file
+        return OpenCvGstCamera(*args, **kwargs)
